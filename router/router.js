const routerConfig = {
    login: '/auth/login',
    register: '/auth/register',
    home: '/',
    dashboard: '/dashboard',
    products: '/dashboard/products',
    users: '/dashboard/users',
};

export default routerConfig;
